server:
  port: 8080

	spring:
	  datasource:
	    url: jdbc:mysql://localhost:3306/nome_database
	    username: tuo_username
	    password: tua_password
	    driver-class-name: com.mysql.cj.jdbc.Driver

	  jpa:
	    database-platform: org.hibernate.dialect.MySQL8Dialect
	    hibernate:
	      ddl-auto: update
	    properties:
	      hibernate:
	        show_sql: true
	        format_sql: true

